#include "so_long.h"

int	deal_key(int key, t_data, *data)
{
	ft_putchar('X');
	return (0);
}

int		main(int argc, char **argv)
{
	t_data *struct;

 	struct = (struct*)malloc(sizeof(struct));
	read_map(struct);
	

}
